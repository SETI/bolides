This directory contains files necessary for Binder to be able to run
interactive notebooks in this repository.

environment.yml sets up the build environment.
postBuild is some commands which run after the environment is created.

Importantly, postBuild actually installs bolides.
